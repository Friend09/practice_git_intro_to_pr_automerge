"""sandbox.app — a deliberately trivial package.

This exists so Gate 2 (CI) has real code to build and test. Its content is not the
point of the curriculum; its presence is. See sandbox/README.md.
"""
